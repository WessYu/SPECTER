import { apiFetch } from "../../../../../lib/api";
import type { FirstPartyDomain } from "../../../../../lib/types";
import { formatDate } from "../../../../../lib/format";
import { ProjectTabs } from "../../../../../components/project-tabs";
import { DomainForm } from "../../../../../components/domain-form";
import { checkDomain } from "../../../actions";

export default async function DomainsPage({params}:{readonly params:Promise<{readonly projectId:string}>}){const {projectId}=await params;const domains=await apiFetch<readonly FirstPartyDomain[]>(`/api/v1/projects/${encodeURIComponent(projectId)}/domains`);return <><div className="page-head"><div><div className="eyebrow">Ownership boundary</div><h1>Domains</h1></div></div><ProjectTabs projectId={projectId}/><section className="panel"><h2>Register a domain</h2><p className="subtle">Passive scans do not require ownership. Verification is the boundary for future capabilities that need stronger authorization.</p><DomainForm projectId={projectId}/></section><section className="section"><div className="section-head"><h2>First-party domains</h2><span className="eyebrow">{domains.length}</span></div>{domains.length===0?<div className="empty"><p className="subtle">No first-party domains registered.</p></div>:<div className="table-wrap"><table><thead><tr><th>Hostname</th><th>Added</th><th>Status</th><th></th></tr></thead><tbody>{domains.map(domain=><tr key={domain.id}><td className="mono">{domain.hostname}</td><td>{formatDate(domain.createdAt)}</td><td>{domain.verifiedAt?<span className="state state-unchanged">Verified {formatDate(domain.verifiedAt)}</span>:<span className="state">Unverified</span>}</td><td className="right">{!domain.verifiedAt?<form action={checkDomain}><input type="hidden" name="projectId" value={projectId}/><input type="hidden" name="domainId" value={domain.id}/><button className="button" type="submit">Check verification</button></form>:null}</td></tr>)}</tbody></table></div>}</section></>}
