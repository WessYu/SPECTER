{
  "name": "@specter/reporter",
  "version": "0.1.0",
  "type": "module",
  "dependencies": { "@specter/types": "workspace:*" },
  "exports": { ".": "./dist/index.js" },
  "types": "./dist/index.d.ts",
  "files": ["dist", "schemas"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --max-warnings=0",
    "test": "vitest run"
  }
}
